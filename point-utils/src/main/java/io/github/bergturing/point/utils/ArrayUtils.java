package io.github.bergturing.point.utils;

/**
 * 数组工具类
 *
 * @author bergturing@qq.com
 */
public class ArrayUtils {
    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(Object[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(boolean[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(byte[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(short[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(char[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(int[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(long[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(float[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为null或者为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean isNullOrEmpty(double[] array) {
        return array == null || array.length == 0;
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(Object[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(boolean[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(byte[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(short[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(char[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(int[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(long[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(float[] array) {
        return !isNullOrEmpty(array);
    }

    /**
     * 数组为不为null并且不为空
     *
     * @param array 数组对象
     * @return 判断结果
     */
    public static boolean nonNullAndEmpty(double[] array) {
        return !isNullOrEmpty(array);
    }
}
